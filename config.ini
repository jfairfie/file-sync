[Local]
target_dir = #Your target directory
upload_dir = #Your upload directory

[GoogleCloud]
project_id = #Project id (don't think this is needed atm so just throw anything here)
bucket_name = #The name of the bucket